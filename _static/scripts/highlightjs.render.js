window.hljs.configure({ languages: [] }); // we need this to prevent highlight.js from infering language types on code blocks and only allow it to highlight those code blocks which already have language-* class
window.hljs.highlightAll();
